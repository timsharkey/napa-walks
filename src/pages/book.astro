---
// The full booking form lives on the homepage at #book so there's only one
// booking form to maintain. This page exists as a stable, bookmarkable /book
// URL (useful for ads/QR codes/etc.) that sends people straight there.
return Astro.redirect('/#book', 307);
---
